export const Encode = ({
  archivo,
  antesArchivo,
  despuesArchivo,
}: {
  archivo: string;
  antesArchivo: string;
  despuesArchivo: string;
}) => {
    
    const enCoding=()=>{
        antesArchivo + archivo + despuesArchivo;
        //SE CAMBIA A ENCODE
    }

};
