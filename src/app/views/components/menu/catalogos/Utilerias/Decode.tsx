export const Decode =(
    {
        archivo,
        antesArchivo,
        despuesArchivo,
      }: {
        archivo: string;
        antesArchivo: string;
        despuesArchivo: string;
      }
)=>{

    const deCoding=()=>{
       // antesArchivo.slice(antesArchivo.length) - archivo - despuesArchivo;
        //SE CAMBIA A ENCODE
    }

}